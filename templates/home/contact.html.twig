{% extends 'base.html.twig' %}

{% block body %}

{# Navbar Desktop #}
{% if app.user %}
{% include('user/sidenav_user.html.twig') %}
{% endif %}

<div class="main" style="background-color: #f1edf6">
    <div class="container-fluid" style="background-color: #f1edf6">
        {# Bloc image #}
        <div class="row">
            <img src="{{ asset('img/fond/hero_area_contact.jpg') }}" class="img-fluid" alt="Photo Sheepi contact">
        </div>
        {# Titre #}
        <div class="row">
            <div class="col-lg-12">
                <h2 class="text-center my-4 title_home">Contact</h2>
            </div>
        </div>
        {# Bloc titre, paragraphe et formulaire de contact #}
        <div class="row justify-content-center" style="background-color: #f1edf6">
            <div class="col-lg-6 col-md-8 text-center bloc_contact mb-4 mx-2">
                <h4 class="titre_contact my-4">Nous sommes à votre écoute :)</h4>
                <div class="col-lg-11 mx-auto mb-4">
                    <p class="paragraphe_contact">
                        Sheepi fait de son mieux pour répondre à vos problèmes, cependant,
                        nous sommes une toute petite entreprise et nous ne pouvons
                        garantir une réponse systématique, mais nous faisons de notre
                        mieux pour vous apporter satisfaction.<br><br>
                        N'hésitez pas à vous rendre dans la <a href="{{ path('home_faq') }}" class="lien_faq">FAQ</a>
                        (foire aux questions), vous verrez elle est très bien faite et vous avez
                        de grandes chances de trouver la solution à votre problème. Vous pouvez
                        également nous faire part de vos ecouragements ou de vos idées
                        d'amélioration. Les bonnes énergies sont toujours les bienvenues :)
                    </p>
                    {{ form_start(form) }}
                    <div class="form-group">
                        {{ form_label(form.nom, 'Nom', { 'label_attr': { 'class': 'float-left' } }) }}
                        {{ form_widget(form.nom, { 'attr': { 'class': 'form-control' } }) }}
                        {{ form_errors(form.nom) }}
                    </div>
                    <div class="form-group">
                        {{ form_label(form.email, 'Email', { 'label_attr': { 'class': 'float-left' } }) }}
                        {{ form_widget(form.email, { 'attr': { 'class': 'form-control' } }) }}
                        {{ form_errors(form.email) }}
                    </div>
                    <div class="form-group">
                        {{ form_label(form.sujet, 'Sujet', { 'label_attr': { 'class': 'float-left' } }) }}
                        {{ form_widget(form.sujet, { 'attr': { 'class': 'custom-select' } }) }}
                        {{ form_errors(form.sujet) }}
                    </div>
                    <div class="form-group">
                        {{ form_label(form.message, 'Message', { 'label_attr': { 'class': 'float-left' } }) }}
                        {{ form_widget(form.message, { 'attr': { 'class': 'form-control' } }) }}
                        {{ form_errors(form.message) }}
                    </div>
                    <button type="submit" class="btn btn-warning btn-block mt-5 mb-5 py-3 bloc_btn">Hop, c'est parti !</button>
                    {{ form_end(form) }}
                </div>
            </div>
        </div>
        {# Navbar mobile #}
        <div class="mt-5 navbar_mobile">
            {% if app.user %}
            {% include('user/navbar_mobile.html.twig') %}
            {% endif %}
        </div>
    </div>
</div>

{% endblock %}